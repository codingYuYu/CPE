#include <bits/stdc++.h>

using namespace std;

int main(){
	int time,a,b;
	cin >> time;
	for(int i = 1;i<=time;i++){
		cin >> a >> b;
		cout << "Case " << i << ": " << a << "+" << b << "=" << a+b << endl;
	}
	return 0;
}