#include <bits/stdc++.h>
using namespace std;
int main(){
	int num;
	while((cin >> num)&& !cin.eof()){
		vector<int> v1(num);
		while(num--){
			cin >> v1[num];
		}
		sort(v1.begin(),v1.end());
		for(int i = 0; i < v1.size();i++){
			if(i){
				cout << " ";
			}
			cout << v1[i];
		}
		cout << "\n";
	}
	return 0;
}
